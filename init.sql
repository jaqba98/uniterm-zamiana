CREATE TABLE IF NOT EXISTS uniterms (
    id INT AUTO_INCREMENT PRIMARY KEY,
    expressionA VARCHAR(255),
    expressionB VARCHAR(255),
    operationSequence VARCHAR(255),
    changeDescription VARCHAR(255),
    fontSize VARCHAR(255)
);
